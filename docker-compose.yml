services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: studysyncer
      POSTGRES_USER: studysyncer
      POSTGRES_PASSWORD: studysyncer
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data:
