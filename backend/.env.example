# Database connection used by Spring Boot (application.properties)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=studysyncer
DB_USER=postgres
DB_PASSWORD=changeme

# Additional CORS origin allowed in production (e.g. https://studysyncer.example.com).
# Leave blank for local dev — http://localhost:3000 is always allowed.
FRONTEND_URL=
